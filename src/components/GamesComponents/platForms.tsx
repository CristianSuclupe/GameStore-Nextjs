"use client";
import { Container } from "semantic-ui-react";
import _ from "lodash";
import { GridGames, Separator } from "../Shared";
import { GameData } from "@/src/api";

type PlatFormsProps = {
  games: GameData[];
};

export const PlatForms = () => {
  return <div>PlatForms</div>;
};
